import React from 'react'

type UseMarkerProps = {
  ref: React.MutableRefObject<mapboxgl.Map | null>
}
const useMarker: React.FC<UseMarkerProps> = ({
  ref,
}) => {
  const addMarker = () => {
    
  }
  return {}
}

export default useMarker